using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy : MonoBehaviour
{
    public static int phantom = 100;

    // Start is called before the first frame update
    void Start()
    {
        phantom = 100;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
